{* =========================================================================
   user-switch-account-forced.tpl — same as user-switch-account but the user
   cannot skip (no account context set yet).
   ========================================================================= *}
{include file="$template/user-switch-account.tpl"}
